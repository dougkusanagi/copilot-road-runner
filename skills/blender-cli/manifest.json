{
  "name": "blender-cli",
  "version": "0.1",
  "description": "Piloto opcional: cenas/objetos simples por scripts parametrizados, com render de verificação.",
  "when": "SOMENTE quando o pedido pedir Blender explicitamente e o modo GUI estiver estável; ambiente descartável separado.",
  "mode": "CLI",
  "tools": ["blender"],
  "args_schema": {
    "recipe": {"enum": ["cubo", "esfera"]},
    "cwd": {"max_len": 40},
    "tamanho": {"max_len": 20}
  },
  "required_args": ["recipe"],
  "arg_order": ["recipe", "tamanho"],
  "scripts": {"cubo": "cubo.py", "esfera": "esfera.py"},
  "effects": ["arquivo .blend + render PNG no cwd delimitado"],
  "verify": ["artefato existe; imagem renderizada validada"]
}
